# estudos-typescript

## Instalação
`npm install -g typescript`
`tsc -v`

## Criar arquivo tsconfig.json
`tsc --init`

## Configurações no tsconfig
```json
"rootDir": "./src"
"outDir": "./dist/js"
```
## Compilar
`tsc`

## Compilação automática
`tsc -w`
